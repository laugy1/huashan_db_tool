# CMS 舊資料清洗建議：events / umaytheater_events 的 event_content.description

**交付對象**：後端工程師
**調查日期**：2026-07-23
**調查範圍**：`events`、`umaytheater_events` 兩個 collection 的 `article.event_content.description` 欄位，**全量**掃描（非抽樣）

---

## 1. 背景

網站上「展演活動」與「Umay 表演節目」的內容資料，有相當比例是從舊系統／舊資料庫匯入，內容是透過 Word、Google Docs 或社群平台（Facebook、Gmail、Apple Mail 等）貼上到 CKEditor 產生的 HTML，帶有大量 inline `style="..."` 與已淘汰的 `<font>` 標籤，還混了各種軟體匯出時特有的殘留標記。

前端目前用統一的 CSS class（`.exhi-single__editor` 內的排版規則）去 render 這段 HTML，這些殘留：

- 會覆蓋前端統一樣式，造成字級、顏色、對齊在不同頁面之間不一致
- 內含大量貼上殘留的無效標記，對畫面完全沒有作用，純粹是雜訊
- 增加 payload 大小、增加前端 sanitize 的負擔

我們希望**未來從資料庫匯入資料時，由後端在匯入階段就清掉這些殘留**，而不是每次都靠前端 sanitize 補救。本文件把目前資料的實際狀況量化列出，並在最後給出明確的實作優先順序，作為後端寫清洗邏輯的依據。

---

## 2. 調查範圍與方法

- 資料來源：正式站 CMS 讀取 API `GET /api/collection_list?id={events|umaytheater_events}&page=&limit=100&lang=zh-TW`，逐頁抓取**全部**資料（非抽樣）
- 欄位路徑：`contentList[].article.event_content.description`（HTML 字串），對應前端 `app/components/exhi/single.vue` 內 `v-html` 渲染的欄位
- 語系：以 `zh-TW` 為主，因為這是資料量的絕大部分（見下表）
- 掃描方式：對每篇 `description` HTML，用正則解析所有 `style="..."` 屬性內容、所有 `<font ...>` 標籤與其屬性，並額外做開放式統計（所有出現過的 HTML 標籤名稱、所有出現過的 `class` 值）
- 「頁數」＝ collection 裡的一筆資料（一篇活動/節目介紹）

### 2.1 資料量與語系分佈

| Collection | 語系 | 總筆數 |
|---|---|---|
| events | zh-TW | 3,477 |
| events | en-US | 6 |
| umaytheater_events | zh-TW | 462 |
| umaytheater_events | en-US | 3 |

`en-US`、`ja-JP` 為內部測試資料（非正式活動內容）；`en-US` 的 9 筆經檢查**完全沒有** inline style 或 `<font>` 標籤，`ja-JP` 未另外掃描，但確認內容乾淨。下述統計數字皆針對 **zh-TW、共 3,939 筆**（正式站內容主要語系）。

---

## 3. 統計結果總覽

| 指標 | events (3,477 筆) | umaytheater_events (462 筆) | 合計 (3,939 筆) |
|---|---|---|---|
| 含 inline `style=` 的筆數 | 1,823 (52.4%) | 378 (81.8%) | 2,201 (55.9%) |
| 含 inline `font-size` 的筆數 | 1,510 (43.4%) | 182 (39.4%) | 1,692 (43.0%) |
| 含 `<font>` 標籤的筆數 | 165 (4.7%) | 15 (3.2%) | 180 (4.6%) |
| `<font>` 標籤總出現次數 | 1,135 | 61 | 1,196 |

**結論先講**：超過一半的資料帶有 inline style，超過四成帶有 inline font-size 覆蓋字級 —— 這不是零星個案，是系統性問題，值得在匯入層處理。

補充：全量掃描確認這批資料**沒有** `<script>` 標籤、也沒有 `onclick=` 等事件屬性，目前沒有 XSS 等級的風險殘留。但這不代表匯入邏輯可以省略基本的標籤過濾——這次乾淨不保證下一批舊資料匯入也一樣乾淨。

---

## 4. Inline `font-size` 數值分佈

### 4.1 px 單位（共 18 種相異數值，含小數點）

| 數值 | 出現次數 |
|---|---|
| 8px | 5 |
| 9px | 9 |
| 10px | 14 |
| 11px | 13 |
| 12px | 240 |
| 13px | 1 |
| **14px** | **4,883** |
| 14.6667px | 5 |
| 15px | 3 |
| **16px** | **3,708** |
| 18px | 920 |
| 20px | 201 |
| 22px | 65 |
| 24px | 84 |
| 26px | 34 |
| 28px | 22 |
| 36px | 11 |
| 48px | 1 |

### 4.2 pt 單位（共 25 種相異數值，含小數點；同一數值不同寫法如 `11.0pt`／`11pt` 已合併計數）

| 數值 | 出現次數 |
|---|---|
| 1pt | 4 |
| 7pt | 8 |
| 7.5pt | 43 |
| 8pt | 243 |
| 8.5pt | 22 |
| 9pt | 836 |
| 9.5pt | 57 |
| 10pt | 4,137 |
| 10.5pt | 456 |
| **11pt** | **5,014** |
| 11.5pt | 2,367 |
| 12pt | 775 |
| 12.5pt | 23 |
| 13pt | 739 |
| 13.5pt | 131 |
| 14pt | 319 |
| 15pt | 55 |
| 15.5pt | 14 |
| 16pt | 40 |
| 16.5pt | 3 |
| 18pt | 47 |
| 19pt | 2 |
| 20pt | 3 |
| 22pt | 10 |
| 25pt | 6 |

### 4.3 其他寫法

| 數值 | 出現次數 | 說明 |
|---|---|---|
| `small` | 147 | CSS 關鍵字，非數值 |
| `medium` | 32 | CSS 關鍵字，非數值 |
| `1.75em` | 2 | 相對單位 |

**觀察**：`pt` 是 Word 貼上時的預設單位（11pt / 10pt 是 Word 常見預設字級），`px` 則多半來自 CKEditor 或 Facebook 貼上。兩種單位、加上關鍵字寫法混雜，數值多達 40+ 種相異值，完全無法對應到前端任何一套字級系統 —— 這也是我們判斷「不值得轉換保留、直接清掉」的原因之一。這些數值都是 inline `style="font-size:..."` 裡的其中一條規則，只要整條 `style` 屬性砍掉就會一併清除，不需要針對 `font-size` 另外寫邏輯。

---

## 5. `<font>` 標籤統計

共 180 筆資料含 `<font>` 標籤（events 165 筆、umaytheater_events 15 筆），總計 1,196 個標籤實例。

`size`、`face`、`color` 是 `<font>` 標籤自己的 HTML 屬性（不是 `style`），只要把整個 `<font ...>` 標籤拿掉（保留裡面的文字），這三個屬性會跟著標籤一起消失，不需要另外處理。範例：

清理前：
```html
<span style="font-size:12px">
  <font color="#e74c3c">工作坊</font>
</span>
```

清理後（`style` 屬性砍掉、`<font>` 標籤整個拿掉只留文字）：
```html
<span>工作坊</span>
```

### 5.1 `size` 屬性（HTML 舊式相對字級 1–7）

| size 值 | 出現次數 |
|---|---|
| size="1" | 2 |
| size="2" | 252 |
| size="3" | 247 |
| size="4" | 65 |
| size="5" | 9 |

### 5.2 `face` 屬性（字體）

| face 值 | 出現次數 |
|---|---|
| Arial | 236 |
| Times New Roman | 47 |
| Verdana | 45 |
| Calibri | 27 |
| microsoft jhenghei, sans-serif | 23 |
| Arial, Helvetica, sans-serif | 11 |
| 微軟正黑體, sans-serif | 7 |
| Courier New | 5 |
| 新細明體 | 4 |
| 新細明體, serif | 4 |
| Heiti TC | 4 |
| "新細明體",serif（HTML 實體跳脫） | 2 |
| Calibri, sans-serif | 2 |
| 其餘（Tahoma / 微軟正黑體 / Georgia / Comic Sans MS 等） | 各 1 |

### 5.3 `color` 屬性（共 37 種相異色值）

出現次數前 10 名：

| color 值 | 出現次數 |
|---|---|
| #0000ff | 116 |
| #000000 | 54 |
| #008080 | 24 |
| #ffffff | 18 |
| #800080 | 15 |
| #e74c3c | 14 |
| #444444 | 13 |
| #ff0000 | 12 |
| #333399 | 7 |
| #339966 | 7 |

其餘 27 種色值各出現 1–6 次不等（含 1 筆空字串 `color=""` 的異常寫法）。

---

## 6. Inline `style` 屬性完整清單與分類

以下是 zh-TW 全量資料中，`style="..."` 屬性裡實際出現過的**所有** CSS 屬性名稱（依「影響筆數」排序），並依性質分成四類 + 雜項。這些屬性會出現在**任何**標籤上（`<p>`、`<span>`、`<div>`、`<td>`、`<li>`... 都有可能），例如：

```html
<p style="font-family:'Times New Roman'; color:#0000ff; text-align:center;">...</p>
<td style="border:1px solid #ccc; padding:5px;">...</td>
```

清洗規則不用分標籤種類，也不用分析屬性內容是分類一~四的哪一種：**只要標籤上有 `style` 屬性，整條屬性砍掉**。

### 6.1 分類一：真正有視覺意義的屬性

這些屬性在瀏覽器裡確實會影響畫面呈現，但因為前端已有統一設計系統（字級用 `typo` mixin、間距用 token），**這些值本來就不該由內容資料決定**，故仍建議整批清除。

| 屬性 | 影響筆數 | 出現次數 |
|---|---|---|
| font-family | 1,390 | 27,959 |
| color | 1,338 | 19,781 |
| text-align | 883 | 8,000 |
| line-height | 646 | 8,607 |
| background | 407 | 6,243 |
| text-justify | 342 | 2,858 |
| border | 204 | 1,633 |
| background-color | 197 | 1,264 |
| margin-bottom | 166 | 1,481 |
| margin-top | 132 | 1,258 |
| margin-left | 127 | 869 |
| margin | 124 | 1,662 |
| padding-bottom / padding | 100 / 100 | 1,123 / 1,707 |
| padding-top | 99 | 1,122 |
| padding-left | 98 | 1,121 |
| padding-right | 97 | 1,120 |
| margin-right | 87 | 571 |
| text-decoration | 82 | 433 |
| letter-spacing | 68 | 802 |
| width | 64 | 910 |
| font-weight | 54 | 572 |
| height | 51 | 645 |
| word-spacing | 47 | 560 |
| text-transform | 46 | 552 |
| font-style | 44 | 493 |
| white-space | 44 | 741 |
| display | 34 | 122 |
| vertical-align | 33 | 315 |
| text-decoration-color / -style | 33 / 33 | 570 / 570 |
| border-bottom / -left / -right / -top | 29 / 26 / 25 / 25 | 503 / 531 / 501 / 631 |
| border-collapse | 25 | 29 |
| text-decoration-thickness | 23 | 271 |
| text-indent | 35 | 261 |
| font（shorthand） | 17 | 153 |
| box-sizing | 9 | 429 |
| position / top / left / z-index | 8 / 5 / 1 / 2 | 110 / 14 / 2 / 3 |
| outline | 6 | 297 |
| float | 6 | 32 |
| list-style-type / list-style / list-style-position | 6 / 1 / 1 | 12 / 1 / 3 |
| background-size / -image / -position / -repeat / -origin / -clip / -attachment | 各 5–7 | 各數十 |
| max-width / min-width | 2 / 3 | 86 / 3 |
| border-color / -style / -width / -image | 各 1–2 | 各數十 |
| word-break / word-wrap | 3 / 2 | 77 / 8 |
| cursor | 5 | 9 |
| overflow / opacity / clear / caret-color | 各 1 | 92 / 1 / 1 / 32 |
| font-variant | 9 | 38 |

### 6.2 分類二：Word / Office 貼上殘留的無效屬性（`mso-*` 系列）

這些是 Microsoft Office 存 HTML 時特有的私有屬性，**標準瀏覽器完全不認得、不會有任何作用**，純粹是雜訊，100% 可以無腦刪除，不需要任何轉換邏輯：

| 屬性 | 影響筆數 | 出現次數 |
|---|---|---|
| mso-bidi-font-family | 17 | 341 |
| mso-font-kerning | 14 | 390 |
| mso-ansi-language | 13 | 81 |
| mso-fareast-language | 12 | 77 |
| mso-bidi-language | 12 | 77 |
| mso-ascii-font-family | 12 | 265 |
| mso-hansi-font-family | 12 | 266 |
| mso-bidi-font-size | 8 | 161 |
| mso-bidi-font-weight | 7 | 75 |
| mso-fareast-font-family | 6 | 143 |
| mso-pagination | 5 | 69 |
| mso-spacerun | 5 | 30 |
| mso-border-insideh / -insidev | 4 / 4 | 4 / 4 |
| mso-yfti-firstrow / -irow / -lastrow / -tbllook | 4 / 4 / 4 / 3 | 4 / 35 / 4 / 3 |
| mso-border-alt / -left-alt / -top-alt | 4 / 3 / 3 | 109 / 55 / 65 |
| mso-padding-alt | 4 | 4 |
| mso-layout-grid-align | 3 | 62 |
| mso-list | 3 | 30 |
| mso-line-height-rule / -alt | 3 / 1 | 78 / 8 |
| mso-para-margin-left / -top / -bottom / -right | 各 1–2 | 各 13–23 |
| mso-fareast-theme-font / -hansi-theme-font / -ascii-theme-font | 各 1–2 | 各 12–14 |
| mso-highlight | 2 | 46 |
| mso-cellspacing / -char-indent-count | 各 1 | 1 / 13 |
| **layout-grid-mode**（Word 延伸屬性，非 mso- 前綴） | 37 | 332 |
| **tab-stops**（Word 延伸屬性） | 35 | 181 |
| **text-autospace**（Word 延伸屬性） | 14 | 96 |
| **-x-system-font**（舊 IE / Word 專屬） | 2 | 8 |
| **-ms-text-justify**（Word 匯出時與 text-justify 成對出現的重複宣告） | 2 | 3 |
| **v-text-anchor**（Word VML 繪圖殘留） | 1 | 2 |

> 這類屬性合計影響 **約 45 筆**資料（實際筆數因同一筆常同時含多個 mso- 屬性而有重疊），但每一條規則都是純垃圾，沒有任何萬用的清除邏輯比「整條丟掉」更好。

### 6.3 分類三：印刷用／瀏覽器忽略的屬性

這些屬性是給「列印」情境用的分頁控制，對網頁畫面呈現沒有作用：

| 屬性 | 影響筆數 | 出現次數 |
|---|---|---|
| orphans | 38 | 474 |
| widows | 38 | 508 |
| page-break-after | 4 | 10 |
| page-break-before | 1 | 1 |
| break-after | 1 | 3 |

### 6.4 分類四：字體渲染細節類（影響極小）

這些是瀏覽器字體渲染引擎的細節開關（連字、東亞變體、字重拉伸、文字描邊寬度等），一般使用者肉眼幾乎無法察覺差異，且都是 Word/CKEditor 匯出時自動帶出的預設值，不是編輯者刻意設定：

| 屬性 | 影響筆數 | 出現次數 |
|---|---|---|
| font-variant-caps / -ligatures | 33 / 33 | 457 / 457 |
| -webkit-text-stroke-width | 49 | 486 |
| font-variant-numeric / -east-asian / -alternates / -position / -emoji | 各 1–2 | 各 18–47 |
| font-kerning / font-feature-settings | 各 2 | 各 64 |
| font-optical-sizing / font-variation-settings | 各 2 | 各 38 |
| font-stretch | 5 | 80 |
| font-size-adjust | 3 | 31 |
| -webkit-text-size-adjust | 1 | 11 |

### 6.5 其他雜項（含明顯解析垃圾）

掃描時發現極少數 style 屬性內容本身就是壞掉的雜訊，並非合法 CSS 宣告，例如：

- `style="FONT-SIZE: 14pt; 藝術銀行: "` —— 一段中文詞語被錯誤地當成 CSS 屬性名稱貼進去（來源不明，推測是複製貼上時挾帶的雜訊文字）
- `style="SPAN: "`、`style="B: "`、`style="div: "` —— 值為空的無意義宣告（常見於 Facebook 貼上文字的殘留包裝）

| 屬性 | 影響筆數 | 出現次數 |
|---|---|---|
| unicode-bidi | 1 | 1 |
| -moz-background-origin / -inline-policy / -clip（過時 Firefox 專屬前綴，現代瀏覽器不支援） | 各 1 | 各 14 |
| `SPAN` / `B` / `div`（解析雜訊，非真實屬性） | 4 / 1 / 1 | 8 / 1 / 2 |
| `藝術銀行`（解析雜訊，非真實屬性） | 1 | 1 |

這組再次證明：這批資料的 inline style 已經髒到連「保留看起來正常的部分」都不划算，全部清掉是最乾淨的做法。

---

## 7. 附帶發現：其他貼上殘留標記（超出原始欄位範圍，供後端參考）

除了 inline `style` 與 `<font>`，開放式掃描這批 `description` 內容還發現不少其他來源的貼上殘留——不只 Word，還有 Google Docs、Gmail、Facebook、Apple Mail 的痕跡。全量資料共出現過 **290 種不同的 HTML 標籤名稱**、**89 種不同的 `class` 值**，扣掉正常標籤後大致分成以下幾組：

### 7.1 Word 完整文件 XML metadata 區塊

`<xml>`、`<w:WordDocument>`、`<w:LatentStyles>` 以及數十個 `<w:xxx>` 設定子標籤（19 頁，各自數十次），實測**大多包在** `<!--[if gte mso 9]>...<![endif]-->` 條件式註解裡。

**是否需要處理**：不用急。IE 條件式註解是 IE 專屬語法，IE9 之後所有現代瀏覽器都不支援，這整段對瀏覽器來說就是一般 HTML 註解，不會被解析、不會進 DOM、不會渲染，無功能/無障礙影響，只是佔資料庫空間。

### 7.2 同樣內容用任意流水號別名重複出現

`<u1:WordDocument>` ~ `<u17:WordDocument>` 這種寫法——同一段 metadata 被貼了好幾次，每次用不同的隨機命名空間別名（`u1`、`u3`、`u5`...）包起來，推測是原始檔案多次貼上/儲存造成的 XML 命名空間衝突處理結果。

**是否需要處理**：同 7.1，也大多包在條件式註解內，無實際影響。

### 7.3 `<w:sdt>` / `<w:sdtPr>`（72 頁、1,838 次，數量最大）

這組**沒有**包在條件式註解裡，是真的會進到 DOM 裡的標籤。實測有一筆帶 `sdttag="goog_rdk_0"`——這其實是 **Google Docs** 匯出時的標記（借用了 Word 的 `w:` 命名空間），證實這批殘留不是只有 Word 一種來源。

**是否需要處理**：不用急。瀏覽器對不認得的自訂標籤會當成 `HTMLUnknownElement`（行為類似匿名 inline 元素），裡面的文字正常顯示，不影響版面；螢幕報讀軟體會忽略標籤本身、照樣讀出文字內容。唯一影響是 HTML 原始碼不合乎標準（跑 W3C 驗證器會報錯），對實際使用者體驗無影響。

### 7.4 Word VML 向量圖形殘留

`<v:shape>`、`<v:path>`、`<v:imagedata>`、`<v:stroke>`、`<v:formulas>`、`<o:lock>` 等（1–2 頁，少量）。VML 是舊版 IE 專屬的向量圖形語法，現代瀏覽器本來就不會渲染，且 Word 通常會同時輸出一個正常 `<img>` 當備援，實際圖片是靠 `<img>` 顯示，VML 部分是死內容。

**是否需要處理**：不用急，同 7.3 判斷。

### 7.5 Word 智慧標籤（Smart Tags）

`<st1:chsdate>`、`<st1:personname>`、`<st1:chmetcnv>`、`<st1:city>`、`<st1:place>`、`<st1:time>`、`<o:smarttagtype>` 等（最多 10 頁）。

**是否需要處理**：不用急，同 7.3 判斷。

### 7.6 head 層級標籤誤貼進內文 —— 建議處理

| 標籤 | 影響筆數 | 出現次數 |
|---|---|---|
| `<meta>` | 49 | 99 |
| `<link>` | 19 | 31 |
| `<style>` | 19 | 140 |
| `<xml>` | 19 | 160 |

這四個是 HTML `<head>` 層級的文件基礎設施標籤（外部資源、meta 資訊、內嵌 CSS），**本來就不該出現在一段內文片段裡**，跟前面「保不保留樣式」的取捨性質不同——不管清不清 inline style，這幾個標籤都不該留。

其中 `<style>` 特別需要注意：如果沒清掉，且前端未來不再依賴 `useSanitize.ts` / `useContentSanitize.ts`（DOMPurify）做最後把關，內嵌的 `<style>` 區塊有可能造成**全站 CSS 汙染**。目前因為前端有 DOMPurify 預設擋掉，沒有實際風險，但建議資料源頭也一併清乾淨，不要只依賴前端這道防線。

### 7.7 Class 殘留（89 種，除了 `Mso*` 系列外還有）

- **Word**：`MsoNormal`、`MsoTableGrid`、`MsoBodyText`、`MsoNoSpacing`、`CxSpFirst/Middle/Last`、`WordSection1`、`TableParagraph`、`SCBBodyCopy*` 等
- **Gmail**：`gmail-p1`~`p5`、一長串亂碼 class 如 `m_6846342846973048134m_164564239931782565...gmail-text_exposed_show`
- **Facebook**：`text_exposed_show`、`profileLink`
- **Apple Mail**：`Apple-converted-space`

**是否需要處理**：不用急。已確認專案 SCSS 沒有定義任何同名全域 class（`.MsoNormal`、`.gmail-p1`、`.a`、`.Table`、`.Title` 等都沒有），這些 class 屬性目前 100% 無視覺效果。唯一潛在風險是「未來如果專案剛好新增同名全域 class」，屆時舊資料可能被意外套到不該套的樣式——這是需要留意的地雷，但不是現在的問題。

---

## 8. 建議實作優先順序

考量「後端要花多少工才划算」，把清洗項目分兩層，避免一次要求太多、拖慢後端排期：

### 8.1 現在就要做（高優先）

| 項目 | 處理方式 |
|---|---|
| inline `style="..."` | 整個屬性移除，不分析內容 |
| `<font>` 標籤 | 移除標籤本身，保留內部文字（`size`/`face`/`color` 屬性一併消失） |
| `<style>` / `<link>` / `<meta>` / `<xml>` 標籤 | 整個標籤移除 |
| `<h1>` 標籤 | 換成 `<h2>`（避免一頁重複出現兩個 `<h1>`；`<h2>` 目前在這個內文區塊完全沒被使用，換階不會撞到既有結構） |

這四項對應第 3~6 節統計出來「影響筆數最大、且有實際風險」的部分，是這次清洗真正的目的。

### 8.2 可以先不做（低優先，附技術理由）

| 項目 | 為什麼可以先放著 |
|---|---|
| `class="Mso*"` / `class="WordSectionN"` | 專案 CSS 沒有同名全域規則，目前無視覺效果 |
| `<o:p>...</o:p>` | 不認得的標籤，瀏覽器當空白 inline 元素處理，文字正常顯示 |
| `<!--[if ...]>...<![endif]-->` 條件式註解 | 現代瀏覽器不支援 IE 條件式語法，整段視為普通 HTML 註解，不解析不渲染 |
| `<w:sdt>` / VML（`<v:shape>` 等）/ Smart Tags（`<st1:*>`） | 同樣是不認得的標籤，`HTMLUnknownElement` 處理，無視覺/無障礙影響 |

這些項目清掉當然更乾淨，但不影響網站功能或無障礙體驗，可以等後端有餘裕時再一併處理，不需要現在納入排期。

---

## 9. 編輯器白名單標籤參考（供未來完整清洗使用）

目前 CMS 文字編輯器（TipTap）啟用的功能，換算成標準 HTML 標籤如下：

```
p, h2, h3, h4, h5, h6, blockquote, hr, pre, code,
strong, em, s, u, a,
ul, ol, li,
img, table, thead, tbody, tr, th, td, br
```

（`h1` 不含在內，保留給頁面標題使用；`br` 換行雖不在編輯器設定分類裡，但幾乎所有設定都預設允許，建議收進白名單一併確認。）

這份清單可以作為**以後**若要做更完整清洗（白名單模式：只保留這個集合裡的標籤，其餘一律砍掉）的依據，不是本次的必做項目，先列在這裡供參考。

---

## 10. 結論與建議

1. **inline style 與 `<font>` 全部清掉，不做任何轉換或保留**：
   - 字級交由前端 CSS（`typo` mixin）統一控制，不從資料裡的 `font-size` / `<font size>` 反推對照表
   - 文字置中等對齊效果不保留（`text-align` / `text-justify` 一併清除）
   - 顏色不保留（inline `color`、`<font color>` 一併清除）
2. **`<style>`/`<link>`/`<meta>`/`<xml>` 一併清掉**：這些標籤本來就不該出現在內文欄位裡，跟「保不保留樣式」是不同性質的問題
3. **`<h1>` 換成 `<h2>`**：避免重複 `<h1>`，`<h2>` 目前未被使用，換階不影響既有結構
4. **不需要為 mso-\* 或字體渲染細節類屬性寫個別白名單/黑名單邏輯**：直接把整個 `style` 屬性砍掉即可
5. **第 7 節裡標記「不用急」的項目可以先不處理**：已確認對網站功能與無障礙沒有實際影響，之後有餘裕再做即可
6. **建議清洗時機**：在「從舊資料庫匯入」的當下做（而非每次讀取都即時 sanitize），這樣才能真正把 CMS 內容清乾淨、減少資料量
7. 目前資料沒有 `<script>` 標籤或事件屬性，沒有 XSS 風險；但匯入邏輯仍建議做基本標籤過濾，不要假設「下一批舊資料匯入也一樣乾淨」

---

## 附錄：範例（原始資料截取）

**Word 表格貼上殘留**（events, `_id: 6a16c4fac9677e7bc59b6cac`）：
```
style="MARGIN: 0cm 0cm 0pt 30pt; TEXT-INDENT: -18pt; LINE-HEIGHT: 150%; tab-stops: list 30.0pt; mso-list: l0 level1 lfo1"
```

**`<font>` 標籤範例**（events, `_id: 6a16c4fac9677e7bc59b6c92`）：
```html
<font size="4">&nbsp;【2009年度特賣資訊】</font>
```

**解析雜訊範例**（events, `_id: 6a16c4fbc9677e7bc59b6ea7`）：
```
style="FONT-SIZE: 14pt; 藝術銀行: "
style="SPAN: "
```

**Google Docs 殘留範例**（events, `_id: 6a16c4ffc9677e7bc59b7307`）：
```html
<span style="font-family:&quot;微軟正黑體&quot;,&quot;sans-serif&quot;">
  <w:sdt id="-1368901258" sdttag="goog_rdk_0">
    <span style="background:white"><span style="color:#1d2129">王品裙</span></span>
  </w:sdt>
</span>
```
